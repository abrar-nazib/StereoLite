% =====================================================================
% CHAPTER 4: Socio-Economic Impact and Sustainability (9-chapter template)
% Assembled per thesis/RESTRUCTURE_PLAN.md from:
%   - old mainmatter/ch4.tex section "Societal, Environmental and
%     Sustainability Aspects", SPLIT: societal/health/safety/legal ->
%     4.2; environment/energy/training footprint -> 4.3.
% New prose: 4.1 Introduction, 4.4 Summary, and the expansion of both
% impact sections. No new numbers beyond the source are introduced.
% POs demonstrated: PO6 (engineer and society), PO7 (environment and
% sustainability).
% =====================================================================
\chapter{Socio-Economic Impact and Sustainability}

\section{Introduction}
The results of this thesis carry practical implications beyond the
benchmark numbers, and this chapter states them. The system under
discussion is the compact stereo matching network designed in Chapter 3,
implemented and deployed in Chapter 7, and evaluated in Chapter 8: a
passive depth sensor built from two commodity image sensors and an
embedded accelerator, whose entire depth computation runs on the device.
Each property of that design, on-device processing, passive sensing, and
a small computational footprint, translates into a concrete societal or
environmental consequence. Section 4.2 examines the societal, health,
safety, legal, and cultural dimensions; Section 4.3 examines the
environmental and sustainability dimensions; Section 4.4 summarizes.

\FloatBarrier
\section{Impact of the Thesis on Societal, Health, Safety, Legal and Cultural Issues}
The most direct societal consequence of this work follows from where the
computation happens. Because the network runs the depth computation on
the device rather than in the cloud, the video stream stays local: no
imagery of the people or spaces that a robot, drone, or headset observes
ever needs to leave the sensor. This matters for privacy in a way that a
cloud-processed depth pipeline cannot match, since a system that
transmits continuous video to a remote server creates, by construction, a
stream of personal imagery that must then be secured, retained, and
governed. Keeping the raw imagery on the device removes that stream at
the source. The same property eases the legal and cultural concerns that
accompany camera-carrying machines. Regulations on personal-data
transfer, and the reasonable discomfort of people who share a space with
an observing device, both attach primarily to imagery that is recorded
or transmitted; an embedded sensor that consumes its own video internally
and emits only geometry is easier to deploy responsibly in homes,
workplaces, and public settings.

Safety is the second dimension. A mobile robot or drone acts safely in
three-dimensional space only to the extent that its depth perception is
reliable, and the evaluation of Chapter 8 addressed exactly the failure
modes that make depth unreliable in the field: the model was tested not
only in its training domain but zero-shot on unseen real scenes, under
controlled rectification error, and on the physical stereo rig itself.
On-device inference also removes the network round trip from the control
loop, so the depth estimate a robot acts on does not depend on the
availability or latency of a wireless link; a platform whose obstacle
avoidance survives a lost connection is categorically safer than one
whose perception stalls with the network. Health enters through the
choice of sensing modality. A passive stereo rig emits no laser and no
structured infrared light; it only observes. There is therefore no
emitted radiation to consider for the eyes of bystanders, which is a
relevant distinction from active depth sensors whose emitters must be
engineered and certified for eye safety, particularly around children
and in crowded indoor spaces.

Finally, the work has an accessibility dimension. A compact, low-cost,
passive depth sensor lowers the barrier to robotics and assistive
technology in settings where specialized depth hardware is unaffordable.
Depth perception built from two commodity image sensors and an embedded
accelerator puts spatial sensing within reach of student projects,
small workshops, assistive devices, and products aimed at price-sensitive
markets, communities that a sensor costing several times as much simply
excludes. Broadening who can build and afford spatially aware machines
is a societal outcome in its own right, and it is a direct consequence of
the design constraints this thesis set out to satisfy.

\FloatBarrier
\section{Impact of the Thesis on the Environment and Sustainability}
The environmental case for on-device stereo begins with energy. The
deployed network operates within a power envelope of a few watts to a few
tens of watts, and computing the depth locally consumes far less energy
per frame than transmitting the video stream to a datacenter and back.
A cloud-processed alternative pays, for every single frame, the energy of
radio transmission, of the network infrastructure along the path, and of
the datacenter inference itself, and it pays this cost for the entire
operating life of the device. On-device inference eliminates that
recurring per-frame transport cost entirely, so the energy advantage
compounds over the deployed lifetime of every unit.

The choice of passive stereo over active sensing has a material
dimension as well. A stereo rig uses two commodity image sensors rather
than a specialized time-of-flight or LiDAR unit, and it emits no laser or
structured light. It therefore carries no emitter hardware at all: no
laser diode, no pattern projector, and none of the associated drive
electronics. Commodity image sensors are manufactured at enormous scale
for the phone and webcam markets, so a depth sensor assembled from them
inherits the low material cost and mature supply chain of that scale,
rather than demanding the specialized components of an active unit. Fewer
and more common parts also mean the sensor is easier to source, repair,
and replace over its service life, which is the unglamorous but real
substance of sustainable hardware.

Sustainability also applies to the development of the model itself, and
this thesis reports its training footprint transparently. The production
model was trained in roughly one day on a single cloud GPU, and the
ablation and evaluation runs each cost a fraction of a dollar of cloud
time. This footprint is deliberately modest by the standards of modern
deep learning, where the largest stereo systems are built on
foundation-scale pretraining whose compute is orders of magnitude
larger. The methodology that made this possible, the small controlled
ablation harness of Chapter 3 that filtered design candidates cheaply
before the single full training run of Chapter 7, is itself a
sustainability practice: compute was spent once, on a design that had
already earned it. The full financial record of this footprint appears
in Chapter 6.

\FloatBarrier
\section{Summary}
This chapter traced the design properties of the system to their
societal and environmental consequences. On-device inference keeps
imagery local, which protects the privacy of observed people and spaces
and eases legal and cultural concerns; reliable, connection-independent
depth makes robots and drones safer; passive sensing emits no laser and
poses no eye-safety hazard; and the low cost of the sensor widens access
to assistive and robotic technology. Environmentally, local computation
avoids the recurring energy cost of a cloud round trip, the passive rig
needs no specialized emitter hardware beyond two commodity image
sensors, and the model's training footprint, roughly one day on a single
cloud GPU with ablations costing a fraction of a dollar each, is small
and reported honestly. These impacts are not add-ons but direct
consequences of the edge-deployment constraints that shaped the design
of Chapter 3.
